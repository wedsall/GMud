-- Define the dragon
dragon = {
        name = params.name,
        health = params.health,
        attack_power = params.attack_power
        -- Add more properties and behaviors as needed
}

return dragon
